{
  "name": "optimus",
  "version": "1.0.0",
  "description": "Governed agent organization — AI agents handle operations under human board oversight",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "cd autobot-inbox && npm start",
    "test": "cd autobot-inbox && npm test",
    "migrate": "cd autobot-inbox && npm run migrate",
    "postinstall": "cd lib && npm install --silent"
  },
  "dependencies": {
    "chrome-launcher": "^1.2.1",
    "playwright": "^1.58.2"
  }
}
